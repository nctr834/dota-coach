[lint.per-file-ignores]
# These insert analyzer/ onto sys.path and chdir before importing from it, so the
# analyzer import must come after the path setup rather than at the top.
"api/main.py" = ["E402"]
"scripts/try_review.py" = ["E402"]
"eval/run_eval.py" = ["E402"]
